apiVersion: v1
kind: Service
metadata:
  name: backend-clusterip
  namespace: dev-project
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  selector:
    app: backend-app
  ports:
    - protocol: TCP
      port: 8085
      name: http