apiVersion: v1
kind: Service
metadata:
  name: mongo-express-clusterip
  namespace: dev-project
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  selector:
    app: mongo-express
  type: ClusterIP  
  ports:
    - protocol: TCP
      port: 8081