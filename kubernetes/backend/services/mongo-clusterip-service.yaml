apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: dev-project
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017