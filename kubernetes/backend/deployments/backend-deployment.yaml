apiVersion: apps/v1
kind: Deployment
metadata:
  name: bakend-app-deployment
  namespace: dev-project
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    app: backend-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-app
  template:
    metadata:
      labels:
        app: backend-app
    spec:
      containers:
        - name: backend-api
          image: dimakhot/backend-app:6daa7ec446fc78710eba1a3e3857152e804a132d
          ports:
            - containerPort: 8085
          resources:
            limits:
              memory: "256Mi"
              cpu: "200m"
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: backend-secret
                  key: MONGO_URI
