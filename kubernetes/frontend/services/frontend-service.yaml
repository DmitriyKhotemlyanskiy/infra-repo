apiVersion: v1
kind: Service
metadata:
  name: frontend-clusterip
  namespace: dev-project
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  selector:
    app: frontend-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000