apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-app-deployment
  namespace: dev-project
  labels:
    app: frontend-app
spec:
  replicas: 5
  selector:
    matchLabels:
      app: frontend-app
  template:
    metadata:
      labels:
        app: frontend-app
    spec:
      containers:
        - name: frontend-app
          image: dimakhot/frontend-py3-app:38727315333d1a56901cafba00ca5703e6f54b28
          ports:
            - containerPort: 3000
          resources:
            limits:
              memory: "256Mi"
              cpu: "200m"
          env:
            - name: API_URL
              valueFrom:
                configMapKeyRef:
                  name: frontend-configmap
                  key: API_URL
