apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-app-deployment
  namespace: dev-project
  labels:
    app: frontend-app
spec:
  replicas: 5
  selector:
    matchLabels:
      app: frontend-app
  template:
    metadata:
      labels:
        app: frontend-app
    spec:
      containers:
        - name: frontend-app
          image: dimakhot/frontend-py3-app:bb34cb5dd1e7c13d209e994bb541b00635c9b5f8
          ports:
            - containerPort: 3000
          resources:
            limits:
              memory: "256Mi"
              cpu: "200m"
          env:
            - name: API_URL
              valueFrom:
                configMapKeyRef:
                  name: frontend-configmap
                  key: API_URL
